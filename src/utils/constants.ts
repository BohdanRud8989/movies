import { PageMeta } from '../types';

export const ORIGIN = 'https://dev.flixforge.com';
export const API_URL = `${ORIGIN}/p/test_movies.pl`;
export const APP_HOST = 'http://localhost:3000';
export const AUTHORIZATION_TOKEN = 'Bearer SecretPass';

export const META: PageMeta = {
    title: 'Wookie movies platform',
    description: 'The most advanced movies platform',
};
